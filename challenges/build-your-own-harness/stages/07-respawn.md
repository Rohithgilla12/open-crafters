# Stage 7: Spawn again

Two `spawn` calls must both succeed and return **different** addresses.
The tester pings each address directly.

Each spawn gets its own port and temp data directory.
---

<!-- crafters-stage-hint -->
## Stuck?

<details>
<summary><strong>Spoiler-free hint</strong></summary>

> **Hint:** Two `spawn` calls must return different addresses. Each child gets its own port and data dir.

Or run: <code>crafters hint harness --stage respawn</code>
</details>
<!-- /crafters-stage-hint -->
