# Stage 6: Bloom membership

After `shorten`, the code must appear in bloom filter `codes` (`maybe_present: true`).
---

<!-- crafters-stage-hint -->
## Stuck?

<details>
<summary><strong>Spoiler-free hint</strong></summary>

> **Hint:** `create` filter `codes` once (ignore `FILTER_EXISTS`); `add` every minted code.

Or run: <code>crafters hint url-shortener --stage bloom</code>
</details>
<!-- /crafters-stage-hint -->
